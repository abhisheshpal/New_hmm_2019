image_publisher.cpp is the ros image publisher
RGB_IR_D_P_stream.cpp saves the image, depth, infrared data

You will need to open 
cpp-config-ui
to configure the gain and exposure so that you get good depth information

After configuring those data, change the corresponding value in the following code
image_publisher.cpp
RGB_IR_D_P_stream.cpp

